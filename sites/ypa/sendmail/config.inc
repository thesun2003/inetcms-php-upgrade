<?php
#  ѕожалуйста, прочитайте файл readme.txt перед тем как что-либо здесь редактировать!

#  URL и название ссылки, кнопка на которую будет по€вл€тьс€ вместе с сообщением об успешной
#  отправке. ƒопускаетс€ использование любых ссылок, например, "/", "./folder/", "/folder/index.htm".
$config['url_name'] = "¬ернутьс€ на главную";
$config['url_path'] = "/";

#  ѕуть к файлу, содержащий список получателей, а также номер получател€ по умолчанию.
$config['recip_f'] = "./recip.txt";
$config['recip_n'] = 1;

#  ѕо умолчанию в письме приход€т данные об IP адресе с которого пришло сообщение.
#  ≈сли это лишнее, то смените значение на "0".
$config['features'] = 0;

#  ѕути к заголовкам. ќбычно подход€т те, что прилагаютс€ к скрипту - при желании вы можете
#  вручную отредактировать header.inc, изменив там название страницы посредством редактировани€
#  тега <title>, а также изменить содержимое заголовка "description".
#  “акже вы можете создать свои заголовки, чтобы вставить скрипт в дизайн вашего сайта.
#  «аголовки состо€т из обычного HTML-файла, разделенного пополам тегами <body>, т.е.
#  файл header.inc должен заканчиватьс€ тегом <body>, а footer.inc начинатьс€ </body>.
#  ¬ажно, чтобы в header.inc был заголовок "content-type" с кодировкой windows-1251.
$config['header'] = "./header.inc";
$config['footer'] = "./footer.inc";

#  ѕоказ копирайта. ≈сли вы очень сильно не хотите, чтобы показывалс€ маленький
#  заголовочек со ссылкой на скрипт, тогда измените значение на "0". ќчень прошу
#  не делать этого :).
$config['copyshow'] = 0;
?>
